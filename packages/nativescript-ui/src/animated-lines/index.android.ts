import { Application, Color, Property, Utils } from '@nativescript/core';
import { AnimatedLinesCommon } from './common';

// export const dataProperty = new Property<CheckboxCommon, Item>({
//   name: "data",
// });

export class AnimatedLines extends AnimatedLinesCommon {
  // nativeViewProtected: it.emperor.animatedcheckbox.AnimatedCheckBox;
  // createNativeView() {
  //   return new it.emperor.animatedcheckbox.AnimatedCheckBox(
  //     Utils.android.getApplicationContext()
  //   );
  // }
}

// dataProperty.register(Checkbox);
